<Window x:Class="QL_DatVeMayBay.GUI.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        Title="Loading System..." Height="420" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="#2BAAE1" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="#000000" Opacity="0.35" Direction="270"/>
        </Border.Effect>

        <Grid Margin="35,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,5,0,0">
                <TextBlock Text="✈ AIRLINE BOOKING SYSTEM" FontSize="26" FontWeight="Black" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                <TextBlock Text="HỆ THỐNG QUẢN LÝ &amp; ĐẶT VÉ MÁY BAY" FontSize="12" FontWeight="Bold" Foreground="#E0F2FE" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,10">
                <Image gif:ImageBehavior.AnimatedSource="/Flight.gif" 
                       Height="250" Width="350"
                       Stretch="Uniform" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
            </Grid>

            <!-- Status & Percentage -->
            <Grid Grid.Row="2" Margin="2,0,2,8">
                <TextBlock x:Name="lblStatus" Text="Đang kết nối Cơ sở dữ liệu SQL Server..." Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold"/>
                <TextBlock x:Name="lblPercent" Text="0%" Foreground="#FFFFFF" FontSize="14" FontWeight="Black" HorizontalAlignment="Right"/>
            </Grid>

            <!-- Progress Bar -->
            <Border Grid.Row="3" Background="#1D89BD" CornerRadius="6" Height="8" Padding="1.5">
                <ProgressBar x:Name="pbLoading" Minimum="0" Maximum="100" 
                             Foreground="#FFFFFF" Background="Transparent" BorderThickness="0">
                    <ProgressBar.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </ProgressBar.Resources>
                </ProgressBar>
            </Border>

            <!-- Footer -->
            <Grid Grid.Row="4" Margin="0,15,0,0">
                <TextBlock Text="© 2026 FLIGHT ENTERPRISE" Foreground="#E0F2FE" FontSize="10" FontWeight="Bold" HorizontalAlignment="Left"/>
                <TextBlock Text="V3.8.0 PROD" Foreground="#E0F2FE" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
    </Border>
</Window>